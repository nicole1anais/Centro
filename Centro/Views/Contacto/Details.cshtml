@model Centro.Models.Contacto

@{
}

<link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;600&display=swap" rel="stylesheet">
<link rel="stylesheet" href="~/css/Contactos/Detalles.css" asp-append-version="true" />

<div class="container mt-5">
    <!-- Botón para volver al mapa -->
    <a href="Contacto/List" class="back-to-map-icon">
        <i class="bi bi-arrow-left-circle-fill"></i>
    </a>

    <h1 class="text-center" style="font-family: 'Poppins', sans-serif; color: #3a3a3a;">Detalles de Contactos</h1>

    <div class="table-responsive mt-4">
        <table class="table table-bordered shadow-sm">
            <thead style="background-color: #ced4db;">
                <tr>
                    <th>Campo</th>
                    <th>Datos</th>
                </tr>
            </thead>
            <tbody>
                <tr>
                    <td>@Html.DisplayNameFor(model => model.Nombre)</td>
                    <td>@Html.DisplayFor(model => model.Nombre)</td>
                </tr>
                <tr>
                    <td>@Html.DisplayNameFor(model => model.Email)</td>
                    <td>@Html.DisplayFor(model => model.Email)</td>
                </tr>
                <tr>
                    <td>@Html.DisplayNameFor(model => model.Mensaje)</td>
                    <td>@Html.DisplayFor(model => model.Mensaje)</td>
                </tr>
            </tbody>
        </table>
    </div>
</div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap-icons/font/bootstrap-icons.css"></script>